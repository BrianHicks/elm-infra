- hosts: all
  gather_facts: false
  strategy: free # since we can't use mitogen until Python is installed

  tasks:
    - name: install python
      raw: test -e /usr/bin/python || (apt-get -y update && apt-get install -y python-minimal)
